#include "Arduino.h"


    // pinMode(pinAirValve, OUTPUT);
    // pinMode(pinOdor1Valve, OUTPUT);
    // pinMode(pinOdor2Valve, OUTPUT);
    // pinMode(pinOdor3Valve, OUTPUT);
    // pinMode(pinOdor4Valve, OUTPUT);
    // pinMode(pinFinalValve, OUTPUT);
    // pinMode(pinWaterDeliveryRight, OUTPUT);
    // pinMode(pinWaterDeliveryLeft, OUTPUT);
    // pinMode(pinBeep, OUTPUT);
    // pinMode(pinBuzz, OUTPUT);

